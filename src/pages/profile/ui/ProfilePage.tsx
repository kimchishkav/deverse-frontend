import { MainLayout } from "@/widgets/layout";

export const ProfilePage = () => {
  return (
    <MainLayout>
      <div style={{ color: "white", fontSize: "32px" }}>Profile Page</div>
    </MainLayout>
  );
};
